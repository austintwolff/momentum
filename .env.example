# Supabase Configuration
# Create a new project at https://supabase.com and copy these values

# Client-side keys (safe to embed in app)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Server-only key (NEVER prefix with EXPO_PUBLIC_)
# Used only by scripts in /scripts folder - never bundled into client
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
